<!DOCTYPE html>
<html lang="<%= currentLang || 'fr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Hana Shortner</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #9333ea;
            --primary-dark: #7928ca;
            --secondary: #e879f9;
            --bg-dark: #0f0f1a;
            --card-bg: #1a1a2e;
            --text-primary: #f8fafc;
            --text-secondary: #cbd5e1;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image:
                radial-gradient(circle at 20% 30%, rgba(147, 51, 234, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(232, 121, 249, 0.15) 0%, transparent 50%);
            z-index: -1;
        }

        .error-container {
            background: var(--card-bg);
            border-radius: 1.5rem;
            padding: 2.5rem;
            box-shadow: 0 8px 32px rgba(147, 51, 234, 0.15);
            border: 1px solid rgba(147, 51, 234, 0.2);
            max-width: 600px;
            width: 90%;
            text-align: center;
        }

        .error-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--secondary), var(--primary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .error-message {
            color: var(--text-secondary);
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .back-button {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            text-decoration: none;
            border-radius: 0.5rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .back-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(147, 51, 234, 0.3);
        }

        @media (max-width: 768px) {
            .error-container {
                padding: 2rem;
            }

            .error-title {
                font-size: 1.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1 class="error-title">
            <%= currentLang === 'en' ? 'Oops! An error occurred' : 'Oops ! Une erreur est survenue' %>
        </h1>
        <p class="error-message"><%= message %></p>
        <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
            <pre style="text-align: left; margin-bottom: 1.5rem; overflow-x: auto; color: var(--text-secondary);">
                <%= error.stack %>
            </pre>
        <% } %>
        <a href="/" class="back-button">
            <%= currentLang === 'en' ? 'Back to Home' : 'Retour à l\'accueil' %>
        </a>
    </div>
</body>
</html>
